{
  "name": "zarr-extensions",
  "version": "1.0.0",
  "description": "Zarr extensions with automated schema documentation",
  "scripts": {
    "update-docs": "node scripts/update-attribute-docs.js",
    "install-hooks": "pre-commit install"
  },
  "devDependencies": {
    "wetzel": "^0.2.3"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
